$kind: collection
description: "Pruebas para todos los modelos del proyecto VetNova. Arrancar en orden: auth → catalogo → inventario → ventas → envio → ficha → agenda → facturacion → notificaciones → laboratorio → reportes → soporte → gateway."
variables:
  token: ""
auth:
  - id: e591978d-989f-4194-8af3-d70fe922ddf7
    type: bearer
    name: bearer auth
    credentials:
      token: "{{token}}"
